# 故障教材专家标注平台 PRD v2.0

> 基于 AgentOS 架构重构 | 2026-05-03

---

## 一、产品概述

### 1.1 背景

故障诊断教材是智能体进行故障规划的核心知识来源。教材质量直接决定诊断报告的准确性和实用性。原有标注流程为线性对话，缺乏智能验证和实时反馈。基于 AgentOS 架构重构后，标注流程升级为 **ReAct 循环 + Hook 机制 + AgentGym 仿真验证** 的智能化闭环。

### 1.2 核心架构定位

```
专家访谈标注技能
    ↓
作为 AgentOS 的一个可复用 Skill
    ↓
封装在 Skills 模块中
    ↓
通过 Hook 机制与故障诊断生成规划报告技能联动
    ↓
通过 AgentGym 进行仿真验证
```

### 1.3 目标用户

| 角色 | 职责 | 使用场景 |
|------|------|---------|
| 领域专家 | 审核修正教材内容 | 对话式修订、风险确认 |
| 知识工程师 | 创建初版教材、管理版本 | AI 生成 + 专家标注协同 |
| 算法工程师 | 分析标注质量、优化诊断模型 | 查看评测报告、仿真结果 |

### 1.4 核心设计原则

1. **对话式 + 智能化验证**：不是简单对话，而是 ReAct 循环（思考→行动→再思考）
2. **Hook 机制护航**：每次关键操作自动触发质量检查、一致性校验
3. **AgentGym 仿真兜底**：标注后自动仿真测试，不合格不让发布
4. **MCP 知识增强**：标注时自动检索案例库、专家库，主动建议优化
5. **ToolProvider 扩展能力**：批量标注、委派任务、规划标注计划

---

## 二、产品架构

### 2.1 整体流程

```
选择产品大类 → 选择/输入故障现象 → AI生成初版教材 → ReAct循环标注 → AgentGym仿真验证 → 发布/修订
```

### 2.2 页面结构

| 页面 | 文件 | 核心功能 |
|------|------|---------|
| 标注平台 | index.html | 教材标注 + ReAct对话 + Hook检查 + AgentGym仿真 |
| 评测平台 | evaluate.html | 四维评测 + 仿真测试 + 修订闭环 |

### 2.3 AgentOS 架构映射

```
┌─────────────────────────────────────────────┐
│              AgentOS Runtime                  │
├──────────┬──────────┬──────────┬────────────┤
│  Skills  │  Hooks   │AgentGym  │ToolProvider│
│          │          │          │            │
│ 标注Skill │ 质量检查  │ 仿真测试  │ Plan       │
│ 评测Skill │ 一致性检查│ 风险预演  │ Delegation │
│ 修订Skill │ 联动同步  │ 诊断模拟  │ BatchAnnotate│
│          │          │          │ KnowledgeSearch│
├──────────┴──────────┴──────────┴────────────┤
│              MCP Integration Layer            │
│  KnowledgeBase | ExpertDB | CaseDB | Training│
└─────────────────────────────────────────────┘
```

---

## 三、标注流程详设

### 3.1 Step 1 - 选择产品大类

| 元素 | 说明 |
|------|------|
| 产品大类卡片 | 挖掘机、泵车、起重机、装载机、摊铺机、压路机 |
| 选择后 | 自动加载该大类下的故障现象列表 |
| MCP联动 | ExpertDatabase 智能推荐当前专家擅长的产品大类 |

### 3.2 Step 2 - 选择/输入故障现象

| 操作 | 规则 |
|------|------|
| 搜索输入 | 支持模糊搜索故障现象 |
| 标签选择 | 展示该大类下已有故障现象标签 |
| 已有教材 | 显示绿色"已有教材"标识 + 版本号 + 最后修改时间 |
| 新增教材 | 显示橙色"新增教材"标识 |
| MCP联动 | CaseDatabase 展示最近相关案例数量 |

### 3.3 Step 3 - AI 生成初版教材

| 阶段 | 说明 |
|------|------|
| 知识检索 | 通过 MCP 调用 KnowledgeBase 检索相关文档 |
| 案例参考 | 通过 MCP 调用 CaseDatabase 获取真实维修案例 |
| 专家参考 | 通过 MCP 调用 ExpertDatabase 匹配领域专家历史标注 |
| 结构生成 | 生成四模块教材：故障现象标题/描述/可能根因/排查步骤 |

### 3.4 Step 4 - ReAct 循环标注（核心）

#### 3.4.1 ReAct 循环机制

原设计（线性对话）：
```
专家修改 → AI理解 → 生成建议 → 专家确认 → 保存
```

新设计（ReAct 循环）：
```
Reasoning: 分析专家修改意图的准确性和必要性
    ↓
Acting: 执行修改生成，提取结构化信息
    ↓
Reasoning: 检查修改是否违反业务规则（步骤顺序、风险等级）
    ↓
Acting: 在 AgentGym 中仿真测试修改后的诊断流程
    ↓
Reasoning: 根据仿真结果判断是否需要专家二次确认
    ↓
Acting: 提交保存或返回调整建议
```

#### 3.4.2 对话区域交互设计

```
┌─────────────────────────────────────────────────┐
│ 💬 ReAct 标注对话                                 │
├─────────────────────────────────────────────────┤
│                                                 │
│ 🤖 AI [Reasoning]:                               │
│    分析您的修改意图...                              │
│    📋 意图识别：删除根因"液压主泵故障"               │
│    🔍 必要性评估：该根因在3次诊断中被排除              │
│    ⚠️ 风险等级：中（该根因被2个其他教材引用）          │
│                                                 │
│ 🤖 AI [Acting]:                                  │
│    执行修改建议生成...                              │
│    ✏️ 建议：将"液压主泵故障"修改为                    │
│       "液压主泵功率控制阀磨损"（更精确）              │
│    📎 MCP-CaseDB: 近3个案例支持此修改                │
│                                                 │
│ 👤 专家:                                         │
│    同意修改，描述改成"液压主泵功率控制阀磨损           │
│    （常见于8吨以上机型）"                            │
│                                                 │
│ 🤖 AI [Reasoning]:                               │
│    检查修改后的一致性...                            │
│    ✅ 与现有教材无冲突                               │
│    🔄 Hook-一致性检查: 通过                         │
│                                                 │
│ 🤖 AI [Acting]:                                  │
│    提交 AgentGym 仿真测试...                       │
│    📊 仿真结果：诊断成功率 92% (↑3%)                │
│    ✅ 仿真通过，无需二次确认                          │
│    [确认修改] [再看看]                              │
│                                                 │
├─────────────────────────────────────────────────┤
│ ToolProvider: [📋Plan] [📤委派] [📦批量] [🔍检索] │
│ MCP知识: [案例库💡] [专家库👤] [知识库📚]            │
│ [输入消息...]                           [发送]    │
└─────────────────────────────────────────────────┘
```

#### 3.4.3 三层标注内容

**第一层：根因列表标注**

| 操作 | 说明 | Hook触发 |
|------|------|---------|
| 保留 | 标记根因为正确 | - |
| 删除 | 标记根因为错误 | 一致性检查：是否被其他教材引用 |
| 修改 | 修改根因描述 | AgentGym仿真：修改后诊断是否正常 |
| 新增 | 添加遗漏根因 | 质量检查：参数标准值是否完整 |

**第二层：剪枝规则标注**

逐段批注模式，每段支持：
- 通过：规则正确
- 修改：需调整规则描述
- 驳回：规则不合理

**第三层：维修建议标注**

同剪枝规则，逐段批注模式。

#### 3.4.4 Hook 机制详解

**Hook 1：质量检查点 (pre_annotation)**

```yaml
触发时机：每次保存标注前 / 修改关键步骤时
检查内容：
  - 步骤完整性（是否有对应的检测步骤）
  - 参数标准值是否完整
  - 风险等级是否合理
通过：允许保存
不通过：返回警告，专家确认后才保存
```

**Hook 2：跨教材一致性检查 (pre_annotation)**

```yaml
触发时机：修改涉及多教材共享的步骤时
检查内容：
  - 查询相关教材的相同故障类型
  - 检查是否存在矛盾或不一致
  - 智能建议：是否同步修改其他教材？
```

**Hook 3：与诊断报告技能联动 (post_annotation)**

```yaml
触发时机：教材标注完成后
动作：
  - 通知故障诊断生成规划报告技能
  - 触发该教材对应诊断报告的重新生成
  - 在 AgentGym 中仿真对比新旧诊断报告的差异
```

#### 3.4.5 ToolProvider 工具

| 工具 | 功能 | 交互示例 |
|------|------|---------|
| **Plan** | 复杂教材的多步骤标注计划 | "我打算分3步改这个教材：1.调整步骤2参数；2.增加检测点；3.补充注意事项" |
| **Delegation** | 委派AI完成批量任务 | "把所有涉及液压系统的教材中，步骤1的压力检测统一成'先测泵出口压力'" |
| **BatchAnnotate** | 批量标注相似教材 | AI自动识别10个相似故障，专家一次性确认 |
| **KnowledgeSearch** | 标注时检索相关知识 | "修改前先看看其他专家怎么标注类似故障" |

#### 3.4.6 MCP 知识增强

| MCP源 | 触发场景 | 展示形式 |
|--------|---------|---------|
| KnowledgeBase | 标注时检索历史记录 | 对话中内嵌"📚 知识库参考"卡片 |
| ExpertDatabase | 任务分配、专家匹配 | 对话中"👤 专家XX曾有类似标注" |
| CaseDatabase | AI主动建议优化 | 对话中"💡 近3个案例都检查了回油管路压力" |
| TrainingMaterial | 新手标注时提供参考 | 侧栏"📖 参考教材"面板 |

### 3.5 Step 5 - AgentGym 仿真验证

标注完成后自动触发 AgentGym 仿真测试：

| 测试项 | 说明 | 失败处理 |
|--------|------|---------|
| 完整度检查 | 教材结构是否完整（现象→原因→步骤→方案） | 返回缺失项清单 |
| 逻辑一致性 | 步骤顺序是否合理，是否存在矛盾 | 返回冲突点 |
| 可达性检查 | 每个步骤的前置条件是否满足 | 返回不可达步骤 |
| 安全性检查 | 高风险操作是否有警告和替代方案 | 阻止发布，强制二次确认 |

仿真报告格式：
```
📊 教材仿真报告：《液压系统行走无力》v2.1
━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 完整度：优秀
⚠️ 逻辑性：良好（1条建议）
✅ 可达性：优秀
⚠️ 安全性：需注意（1条风险）
━━━━━━━━━━━━━━━━━━━━━━━━━━
综合评分：85/100
是否通过：是（需确认风险项）
━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 3.6 Step 6 - 发布/修订

| 操作 | 条件 | 结果 |
|------|------|------|
| 发布 | AgentGym全部通过 | 教材版本+1，触发Hook3联动 |
| 有条件发布 | 安全性需确认 | 专家确认风险项后发布 |
| 返回修订 | 仿真不通过 | 返回Step4继续ReAct循环 |

---

## 四、教材版本管理

### 4.1 版本规则

| 版本 | 触发方式 | 说明 |
|------|---------|------|
| v1.0 | 初次标注完成 | AI生成 + 专家标注定稿 |
| v1.x | 修订任务合并 | 评测反馈的小幅调整 |
| v2.0 | 重大修订 | 多个冲突积累后的全面更新 |

### 4.2 版本数据结构

```json
{
  "textbookId": "TB-EXC-001",
  "version": "2.1",
  "productName": "挖掘机",
  "faultPhenomenon": "液压系统行走无力",
  "status": "published | draft | under_review",
  "agentgymScore": 85,
  "revisionHistory": [
    {
      "version": "2.0",
      "date": "2026-05-02",
      "author": "张工",
      "changes": "删除1个根因，修改2条剪枝规则",
      "agentgymResult": "pass",
      "hookResults": ["quality:pass", "consistency:pass", "sync:triggered"]
    }
  ]
}
```

---

## 五、知识冲突管理

### 5.1 冲突类型

| 冲突类型 | 触发条件 | 修订建议 | Hook响应 |
|---------|---------|---------|---------|
| 根因冲突 | 评测标注"错误根因"/"遗漏根因" | 标记应删除/新增的根因 | 一致性检查：是否影响关联教材 |
| 路径冲突 | 评测标注排查步骤"错误路径" | 标记错误点+建议正确路径 | AgentGym仿真：修改后诊断是否正常 |
| 关联冲突 | 一个教材根因被删改，影响共享根因的其他教材 | 列出受影响教材 | 联动同步：自动通知关联教材维护者 |

### 5.2 修订任务生成

评测完成后自动从评测标注构建修订建议：

```json
{
  "revisionId": "REV-20260503-001",
  "sourceType": "evaluation",
  "sourceId": "EVAL-20260503-003",
  "conflictType": "root_cause | path | association",
  "priority": "high | medium | low",
  "status": "pending | in_progress | accepted | rejected | merged",
  "evidence": {
    "sessionId": "会话ID",
    "reportSnippet": "评测中AI生成的报告片段",
    "expertAnnotation": "专家评测标注"
  },
  "suggestion": {
    "action": "delete | add | modify",
    "targetItem": "具体修改项",
    "originalContent": "原内容",
    "suggestedContent": "建议内容"
  },
  "agentgymValidation": {
    "tested": true,
    "score": 88,
    "passed": true
  }
}
```

### 5.3 修订模式

| 模式 | 适用场景 | 交互方式 |
|------|---------|---------|
| 快速审核 | 冲突少、修改明确 | 表单勾选，逐条接受/拒绝 |
| 对话式修订 | 冲突多、需深入讨论 | AI引导ReAct循环，自然语言交互 |

两种模式均通过 ReAct 循环完成，对话式修订只是多了 AI 的主动推理和仿真验证环节。

---

## 六、UI 设计规范

### 6.1 主题色

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 | #3B82F6 (blue-500) | 主按钮、选中态、链接 |
| 成功 | #10B981 (emerald-500) | 通过、完成、保留 |
| 警告 | #F59E0B (amber-500) | 待确认、Hook警告 |
| 危险 | #EF4444 (red-500) | 错误、删除、仿真失败 |
| AI消息 | #EEF2FF (indigo-50) | AI对话气泡背景 |
| Reasoning | #FEF3C7 (amber-50) | 推理阶段标识 |
| Acting | #DBEAFE (blue-50) | 执行阶段标识 |
| Hook | #F0FDF4 (green-50) | Hook检查通过 |
| AgentGym | #FDF4FF (purple-50) | 仿真结果 |

### 6.2 特殊组件

| 组件 | 说明 |
|------|------|
| ReAct标识 | AI消息左上角显示 [Reasoning] 或 [Acting] 标签 |
| Hook提示条 | 黄色/绿色横幅，展示Hook检查结果 |
| AgentGym面板 | 右侧可展开面板，展示仿真测试进度和结果 |
| ToolProvider工具栏 | 对话输入框上方，4个快捷工具按钮 |
| MCP知识卡片 | 对话中内嵌的小卡片，展示案例/专家/知识库引用 |

---

## 七、后续规划

### P1（高优先级）

- ReAct 循环标注核心流程
- Hook 质量检查 + 一致性检查
- AgentGym 基础仿真（完整度+逻辑性）
- 教材版本管理

### P2（中优先级）

- ToolProvider 全部工具（Plan/Delegation/BatchAnnotate/KnowledgeSearch）
- MCP 四源集成（KnowledgeBase/ExpertDB/CaseDB/TrainingMaterial）
- 修订闭环（评测→修订→验证→发布）
- 对话式修订模式

### P3（低优先级）

- 多人协作标注
- 标注质量评分体系
- 跨技能联动自动化
- AgentGym 高级仿真（安全性+风险预演）
