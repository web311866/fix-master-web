# 故障教材专家标注平台 PRD

## 1. 产品概述

### 1.1 产品背景

工程机械设备的故障诊断高度依赖领域专家经验。当前，故障教材（包含根因分析、剪枝规则、维修建议）的编写与审核采用基于 OpenCode Agent 的 CLI 交互模式，仅技术人员可操作，专家参与门槛高、效率低。

本产品旨在将该 Agent 交互流程转化为可视化 Web 界面，让领域专家能够以图形化、引导式的方式完成故障教材的标注、审核与修正工作，降低操作门槛，提升标注效率和质量。

### 1.2 产品定位

面向工程机械领域专家的**故障教材标注与审核工具**，核心价值：

- 将 CLI Agent 交互流程可视化，降低专家参与门槛
- 通过三层分级标注机制，确保教材质量（根因准确性、剪枝规则合理性、维修建议可操作性）
- 支持已有教材的迭代优化和新教材的从零创建

### 1.3 目标用户

| 角色 | 描述 | 核心诉求 |
|------|------|----------|
| 领域专家 | 工程机械维修/诊断方向的资深工程师 | 高效审核和修正 AI 生成的教材内容 |
| 教材管理员 | 负责教材质量管控的管理人员 | 追踪标注进度、统计标注质量 |
| 知识工程师 | 负责故障知识库构建与维护的人员 | 批量创建和更新故障教材 |

---

## 2. 产品架构

### 2.1 整体流程

```
选择产品大类 → 选择/输入故障现象 → AI 生成初版教材 → 三层标注 → 完成提交
     ①              ②                    ③             ④          ⑤
```

### 2.2 页面结构

| 页面/步骤 | 功能 | 核心交互 |
|-----------|------|----------|
| Step 1 选择产品大类 | 选择设备类型 | 卡片点选 |
| Step 2 故障现象选择 | 搜索/选择/输入故障 | 搜索+标签选择+自动判重 |
| Step 3 AI 生成教材 | 生成根因/剪枝规则/维修建议 | 进度展示+生成摘要 |
| Step 4 三层标注 | 分层审核标注教材内容 | 勾选/删除/编辑/批注 |
| Step 5 完成提交 | 汇总统计+继续下一个 | 统计卡片+操作按钮 |

---

## 3. 功能详设

### 3.1 Step 1：选择产品大类

#### 3.1.1 功能描述

用户首先选择产品大类（设备类型），系统根据不同大类提供对应的故障现象列表和教材数据。

#### 3.1.2 交互规则

| 规则 | 说明 |
|------|------|
| 大类列表 | 以卡片网格展示，每个卡片包含设备图标、名称、已有教材数量 |
| 选择方式 | 单选，点击卡片选中，高亮显示 |
| 默认状态 | 未选择任何大类，"下一步"按钮禁用 |
| 已有大类 | 挖掘机、泵车、起重机、装载机、摊铺机、压路机 |

#### 3.1.3 数据结构

```
大类 {
  name: string          // 大类名称，如"挖掘机"
  icon: SVG             // 设备图标
  materialCount: number // 已有教材数量
}
```

---

### 3.2 Step 2：选择/输入故障现象

#### 3.2.1 功能描述

用户从已有故障列表中选择或手动输入新的故障现象。系统自动判断该故障是否已有教材，并展示对应状态。

#### 3.2.2 交互规则

| 规则 | 说明 |
|------|------|
| 搜索输入 | 支持关键词模糊搜索，输入时过滤标签列表 |
| 标签选择 | 展示当前大类下的所有故障现象标签，点击选中 |
| 已有教材标识 | 绿色圆点标记，标签样式区分 |
| 新增教材标识 | 橙色圆点标记，提示将新建教材 |
| 自由输入 | 用户可输入不在列表中的故障现象，系统默认判定为新增 |
| 自动判重 | 选择故障后，系统自动判断是否已有教材，展示对应状态卡片 |

#### 3.2.3 状态卡片

**已有教材（修改模式）**：
- 标题：已有教材，进入修改模式
- 展示信息：根因数量、剪枝规则数量、维修建议数量
- 元数据：作者、版本号、最后更新时间

**尚未创建（新增模式）**：
- 标题：尚未创建教材，将新增
- 说明：AI 将根据故障描述自动生成初版教材

#### 3.2.4 已完成标注区域

页面底部展示当前会话中已完成标注的故障列表，每项显示故障名称和标注完成时间。

#### 3.2.5 数据结构

```
故障现象 {
  name: string     // 故障名称，如"液压系统行走无力"
  exists: boolean  // 是否已有教材
}

教材元数据 {
  author: string      // 作者
  version: string     // 版本号
  lastUpdate: string  // 最后更新时间
}
```

---

### 3.3 Step 3：AI 生成初版教材

#### 3.3.1 功能描述

系统调用 AI 模型，基于故障现象自动生成包含根因分析、剪枝规则和维修建议的初版教材。

#### 3.3.2 交互规则

| 规则 | 说明 |
|------|------|
| 生成进度 | 展示进度条和阶段文案，模拟生成过程 |
| 阶段文案 | "正在分析故障现象..." → "正在生成根因分析..." → "正在生成剪枝规则..." → "正在生成维修建议..." → "生成完成" |
| 生成摘要 | 完成后展示：根因数量、剪枝规则数量、维修建议数量 |
| 预览内容 | 折叠展示生成的根因/剪枝规则/维修建议摘要 |
| 修改模式 | 若为已有教材，AI 在原基线上进行增量更新 |

#### 3.3.3 数据结构

```
AI 生成结果 {
  rootCauses: RootCause[]         // 根因列表
  pruningRules: PruningRule[]     // 剪枝规则列表
  repairSuggestions: RepairSuggestion[]  // 维修建议列表
}
```

---

### 3.4 Step 4：三层标注

这是产品的核心功能模块，将 AI 生成的教材按三个层级拆分，供专家逐层审核标注。

#### 3.4.1 第一层：根因列表标注

**目的**：审查 AI 生成的根因列表，剔除错误根因、增加遗漏根因、修改描述不准确之处。

**交互规则**：

| 规则 | 说明 |
|------|------|
| 列表展示 | 每条根因展示：序号、标题、AI 建议说明、状态标签 |
| 勾选操作 | 支持多选，勾选后可批量操作 |
| 单条操作 | 每条根因提供：保留、删除、编辑 三个操作按钮 |
| 批量操作 | 顶部提供"批量保留选中"和"批量删除选中" |
| 编辑弹窗 | 点击编辑弹出 Modal，可修改根因描述 |
| 新增根因 | 底部输入框，支持输入遗漏的根因并添加 |
| 状态流转 | pending → kept（保留）/ deleted（已删除）/ edited（已修改）/ added（新增） |
| 实时统计 | 底部实时展示各状态数量统计 |

**根因状态定义**：

| 状态 | 含义 | 颜色标识 |
|------|------|----------|
| pending | 待审核 | 灰色 |
| kept | 保留不变 | 绿色 |
| deleted | 已删除 | 红色 |
| edited | 已修改 | 蓝色 |
| added | 新增 | 青色 |

**数据结构**：

```
根因 {
  id: number
  title: string           // 根因标题
  desc: string            // AI 建议说明
  aiSuggestion: string    // AI 建议（如"建议保留"/"建议删除"）
  status: 'pending' | 'kept' | 'deleted' | 'edited' | 'added'
  editedContent?: string  // 修改后的内容（仅 edited 状态）
}
```

#### 3.4.2 第二层：剪枝规则标注

**目的**：将 AI 生成的剪枝规则按段展示，专家逐段审核批注。

**交互规则**：

| 规则 | 说明 |
|------|------|
| 逐段展示 | 每条剪枝规则作为一个独立段落卡片展示 |
| 段落内容 | 包含标题和详细文本 |
| 操作选项 | 每段提供：通过（Approve）、修改（Edit）、驳回（Reject） |
| 修改批注 | 点击"修改"弹出编辑区域，可修改内容并添加批注说明 |
| 驳回原因 | 驳回时需填写驳回原因 |
| 状态流转 | pending → approved / edited / rejected |

**段落状态定义**：

| 状态 | 含义 | 颜色标识 |
|------|------|----------|
| pending | 待审核 | 灰色 |
| approved | 通过 | 绿色 |
| edited | 已修改 | 蓝色 |
| rejected | 已驳回 | 红色 |

**数据结构**：

```
剪枝规则 {
  id: number
  title: string    // 规则标题
  text: string     // 规则详细内容
  status: 'pending' | 'approved' | 'edited' | 'rejected'
  editedContent?: string   // 修改后内容
  annotation?: string      // 批注/驳回原因
}
```

#### 3.4.3 第三层：维修建议标注

**目的**：与第二层同理，逐段审核批注维修建议。

**交互规则**：与第二层剪枝规则标注一致。

**数据结构**：

```
维修建议 {
  id: number
  title: string    // 建议标题
  text: string     // 建议详细内容
  status: 'pending' | 'approved' | 'edited' | 'rejected'
  editedContent?: string   // 修改后内容
  annotation?: string      // 批注/驳回原因
}
```

#### 3.4.4 层级切换

- 顶部 Tab 切换三层标注视图
- 每个 Tab 显示对应层级的待处理数量 Badge
- 每层底部提供"返回上一层"和"确认并进入下一层"按钮
- 第三层完成后显示"完成标注并提交"按钮
- 各层独立保存状态，支持回退修改

---

### 3.5 Step 5：完成标注

#### 3.5.1 功能描述

展示本次标注的汇总统计信息，用户可选择查看详情或继续标注下一个故障。

#### 3.5.2 交互规则

| 规则 | 说明 |
|------|------|
| 完成提示 | 大号勾选图标 + "标注完成"标题 |
| 汇总统计 | 三张统计卡片：根因标注统计、剪枝规则标注统计、维修建议标注统计 |
| 统计维度 | 每层展示：保留/通过数、删除/驳回数、修改数、新增数 |
| 查看详情 | 弹窗展示本次标注的完整变更明细 |
| 继续标注 | 返回 Step 2，选择下一个故障现象，已完成故障在列表中标记 |

#### 3.5.3 数据结构

```
标注统计 {
  rootCauses: { kept: number, deleted: number, edited: number, added: number }
  pruningRules: { approved: number, rejected: number, edited: number }
  repairSuggestions: { approved: number, rejected: number, edited: number }
}
```

---

## 4. 全局交互规范

### 4.1 步骤导航

| 规则 | 说明 |
|------|------|
| 顶部进度条 | 展示 5 个步骤节点，当前步骤高亮，已完成步骤打勾 |
| 连接线 | 已完成步骤之间的连接线变为实线 |
| 步骤不可跳转 | 用户必须按顺序完成各步骤，不可跳跃 |
| 支持回退 | 可通过"返回"按钮回到上一步，已填写数据保留 |

### 4.2 弹窗规范

| 弹窗类型 | 触发场景 | 内容 |
|----------|----------|------|
| 编辑根因 | 点击根因的"编辑"按钮 | 标题 + 文本编辑区 + 取消/确认 |
| 编辑段落 | 点击剪枝规则/维修建议的"修改"按钮 | 标题 + 文本编辑区 + 批注输入 + 取消/确认 |
| 驳回原因 | 点击"驳回"按钮 | 原因输入区 + 取消/确认 |
| 标注详情 | 完成页点击"查看标注详情" | 完整变更明细列表 |

### 4.3 提示消息（Toast）

| 类型 | 场景 |
|------|------|
| success | 操作成功（删除根因、保留根因、提交标注等） |
| warning | 注意事项（批量操作前的提醒） |
| error | 操作失败 |
| info | 状态变化提示（选择了大类、选择了故障等） |

### 4.4 确认机制

| 场景 | 确认方式 |
|------|----------|
| 删除根因 | 直接执行，Toast 提示 |
| 批量删除 | 直接执行，Toast 提示删除数量 |
| 提交标注 | 直接执行，进入完成页 |

---

## 5. UI 设计规范

### 5.1 主题色

| 用途 | 色值 | 说明 |
|------|------|------|
| 主背景 | #f5f7fa | 浅灰色 |
| 卡片背景 | #ffffff | 白色 |
| 输入框背景 | #f0f2f5 | 浅灰色 |
| 主强调色 | #4f6ef7 | 蓝色，用于主按钮、选中态 |
| 成功色 | #22c55e | 绿色，保留/通过 |
| 警告色 | #f59e0b | 橙色，新增/注意 |
| 危险色 | #ef4444 | 红色，删除/驳回 |
| 修改色 | #4f6ef7 | 蓝色，已修改 |
| 新增色 | #06b6d4 | 青色，新增项 |

### 5.2 字体

- 西文：Inter
- 中文：Noto Sans SC
- 回退：-apple-system, sans-serif

### 5.3 圆角

| 元素 | 圆角 |
|------|------|
| 卡片 | 10px |
| 按钮 | 6px |
| 输入框 | 6px |
| 弹窗 | 14px |

### 5.4 响应式

| 断点 | 布局 |
|------|------|
| ≥1200px | 大类卡片 3 列，内容区域最大宽度 1200px 居中 |
| 768-1199px | 大类卡片 2 列 |
| <768px | 大类卡片 1 列，操作按钮全宽 |

---

## 6. 数据流转

### 6.1 核心数据流

```
用户选择大类
    ↓
加载该大类下的故障现象列表
    ↓
用户选择/输入故障现象 → 系统判重（是否已有教材）
    ↓
调用 AI 接口生成初版教材（或基于已有教材增量更新）
    ↓
第一层标注：根因列表审核（保留/删除/编辑/新增）
    ↓
第二层标注：剪枝规则逐段批注（通过/修改/驳回）
    ↓
第三层标注：维修建议逐段批注（通过/修改/驳回）
    ↓
汇总提交 → 保存标注结果 → 返回 Step 2 继续下一个
```

### 6.2 状态持久化

- 标注过程中各层状态实时保存在前端状态中
- 支持在同一会话内回退修改已标注的层级
- 完成提交后，标注结果持久化到后端

---

## 7. 知识冲突管理与对话式修订

### 7.1 背景

故障教材标注完成后，经评测平台实战验证，可能发现教材内容存在不足（如遗漏根因、错误路径等）。需要一套机制将评测结论反哺回教材修订，且不破坏已有标注的完整性。

**核心原则**：评测不直接改教材，而是生成「修订任务」，由专家审核确认后才生效，确保每次变更都有据可查、有专家背书。

### 7.2 冲突类型与处理策略

| 冲突类型 | 触发条件 | 修订建议内容 | 标注页面响应 |
|---------|---------|------------|-----------|
| **根因冲突** | 评测标注了"错误根因"/"遗漏根因"/"非必要根因" | 标记应删除/应新增/应降级的根因，附带评测会话证据 | 根因列表中出现"待修订"标签，预填修订方向，专家确认后生效 |
| **路径冲突** | 评测标注了排查步骤的"错误路径"并填写了"正确路径" | 标记原路径错误点 + 建议的正确路径 | 剪枝规则/维修建议中出现"待修订"标签，显示路径对比，专家确认后生效 |
| **关联冲突** | 一个故障教材的根因被删改，可能影响共享同一根因的其他教材 | 列出受影响的关联教材 | 关联教材出现"关联待审"标签，提醒专家评估是否需要联动修改 |

### 7.3 修订任务数据结构

```json
{
  "revisionId": "REV-20260503-001",
  "sourceEvaluateId": "EVAL-20260503-003",
  "faultPhenomenon": "液压系统行走无力",
  "conflictType": "root_cause | path | association",
  "priority": "high | medium | low",
  "status": "pending | accepted | rejected | merged",
  "evidence": {
    "sessionId": "会话ID",
    "reportSnippet": "评测中AI生成的报告片段",
    "expertAnnotation": "专家在评测中的标注内容"
  },
  "suggestion": {
    "action": "delete | add | modify",
    "targetItem": "具体要修改的根因/步骤",
    "originalContent": "原教材内容",
    "suggestedContent": "建议修改为"
  },
  "createdAt": "2026-05-03T15:30:00Z",
  "resolvedAt": null,
  "resolvedBy": null,
  "rejectReason": null
}
```

### 7.4 修订模式

专家可选择两种修订模式，最终产出相同（结构化修订结果），区别在于交互路径：

| 模式 | 适用场景 | 交互方式 |
|------|---------|---------|
| **快速审核模式** | 冲突少、修改明确 | 表单勾选，逐条接受/拒绝 |
| **对话式修订模式** | 冲突多、需要深入讨论 | AI 引导式访谈，自然语言交互 |

### 7.5 快速审核模式

#### 7.5.1 交互流程

1. 进入修订任务列表，展示所有待审核修订任务
2. 每条修订任务显示：冲突类型、优先级、评测证据摘要、建议操作
3. 专家对每条任务选择：接受 / 拒绝 / 修改后接受
4. 选择"修改后接受"时，弹出编辑框调整建议内容
5. 选择"拒绝"时，需填写拒绝原因
6. 全部审核完成后，合并到教材，版本号 +1

#### 7.5.2 界面元素

| 元素 | 说明 |
|------|------|
| 修订任务卡片 | 展示冲突类型标签（根因/路径/关联）、优先级标签、来源评测ID |
| 证据区域 | 折叠展示评测证据（会话片段 + 专家标注内容） |
| 建议操作 | 结构化展示：操作类型（删除/新增/修改）+ 原文 + 建议内容 |
| 审核按钮组 | 接受（绿色）/ 拒绝（红色）/ 修改后接受（蓝色） |
| 批量操作 | 支持全选接受、全选拒绝 |

### 7.6 对话式修订模式

#### 7.6.1 设计理念

AI 扮演**修订访谈员**角色，主动提问、引导决策、确认结论。模拟一个"拿着评测报告逐条请教专家"的助手，让专家可以自然语言表达，由 AI 解析意图并结构化记录。

#### 7.6.2 访谈规则

| 规则 | 说明 |
|------|------|
| AI 先给上下文 | 每次提问前，展示评测证据，让专家在充分信息下决策 |
| 一次只问一件事 | 不把多个冲突混在一起，逐条推进 |
| 专家自由表达 | 不限于"是/否"，可以说"不太对，应该是..."，AI 负责解析意图 |
| AI 复述确认 | 专家表达后，AI 用结构化语言复述，确保理解一致 |
| 允许回退 | 专家可以说"刚才那条我再想想"，回退修改 |
| 阶段总结 | 每个模块（根因/路径）结束后，AI 汇总所有修订，一次性确认 |

#### 7.6.3 对话交互示例

```
AI: 我们从评测反馈开始。液压系统行走无力的教材中，
    评测发现根因列表可能存在问题，共3处冲突需要您确认。
    先看第一条——

    📋 评测标注："液压主泵故障"被标记为【错误根因】
    📎 证据：在3次智能体会话中，该根因均被排除，
           实际故障点在行走马达溢流阀。

    您怎么看？这个根因是否确实应该从教材中移除？

专家: 这个根因太笼统了，不是错误，是描述不准确，
      应该改成"液压主泵功率控制阀磨损"

AI: 明白，您的判断是——不是删除，而是修改描述。
    我将修订建议调整为：

    ✏️ 操作：修改描述
    原文："液压主泵故障"
    改为："液压主泵功率控制阀磨损"

    确认这个调整吗？

专家: 确认

AI: ✅ 已记录。接下来看第二条——...
```

#### 7.6.4 页面布局

```
┌────────────────────────────────────────────────┐
│ 修订访谈 - 液压系统行走无力       进度 2/5      │
├──────────────┬─────────────────────────────────┤
│              │                                 │
│  修订任务列表  │     对话区域（主交互区）           │
│              │                                 │
│ ◉ 根因#1 错误 │  AI: 先看第一条...               │
│   [进行中]    │  📋 评测标注：...               │
│ ○ 根因#2 遗漏 │  📎 证据：...                  │
│ ○ 路径#3 顺序 │  您怎么看？                     │
│ ○ 路径#5 遗漏 │                                │
│ ○ 根因#4 非必要│  ────────────────────           │
│              │  专家: 这个根因太笼统...          │
│ ──────────  │                                │
│ 已确认: 0/5   │  AI: 明白，修订建议调整为...     │
│ 当前模块: 根因 │  ✏️ 操作：修改描述              │
│              │  原文："液压主泵故障"             │
│              │  改为："液压主泵功率控制阀磨损"    │
│              │  确认？ [确认] [调整]             │
│              │                                 │
│              │  ────────────────────           │
│              │  [输入消息...]        [发送]      │
├──────────────┴─────────────────────────────────┤
│ 快捷操作: [回退上条] [查看原教材] [查看完整报告]   │
└────────────────────────────────────────────────┘
```

左侧为修订任务导航（可点击跳转），右侧为 AI 对话区。关键交互：
- 专家在输入框自然语言回复
- AI 消息中嵌入结构化信息（证据、建议操作）
- 确认按钮内嵌在对话流中
- 左侧任务列表实时更新状态
- 底部快捷操作栏提供辅助功能

#### 7.6.5 路径冲突的对话式修订

路径冲突涉及步骤顺序和逻辑关系，访谈更结构化：

```
AI: 接下来是排查路径的问题。评测发现第3步有误——

    📋 原路径步骤3："检查主泵压力"
    ❌ 评测标注：【顺序错误】应在检查行走先导压力之后
    📎 专家标注的正确路径："先检查行走先导压力，再检查主泵压力"

    您同意这个调整吗？

专家: 同意，先导压力是前提条件

AI: 明白。那这两步之间是否需要增加其他步骤？
    比如在检查先导压力后，是否应该先看压力值再决定下一步？

专家: 对，应该加一个判断分支：
      如果先导压力正常→进入主泵检查
      如果先导压力不足→排查先导油路

AI: 理解了，这是修订后的路径片段：

    步骤3: 检查行走先导压力
      ├─ 若正常 → 步骤4: 检查主泵压力
      └─ 若不足 → 步骤4': 排查先导油路

    确认这个调整吗？

专家: 确认
```

### 7.7 修订入口

#### 7.7.1 标注页面入口

- 顶部导航栏增加"修订任务"按钮，带未处理数量徽章
- 点击进入修订任务列表页
- 每条任务可选择"快速审核"或"对话式修订"

#### 7.7.2 评测页面入口

- 评测完成后（Step 7），增加"生成修订任务"按钮
- 系统自动将评测标注转化为修订建议
- 提交前可预览修订任务列表
- 已生成的修订任务状态可追踪

### 7.8 教材版本管理

| 版本 | 触发方式 | 说明 |
|------|---------|------|
| v1.0 | 初次标注完成 | AI 生成 + 专家标注定稿 |
| v1.1 | 修订任务合并 | 评测反馈的小幅调整 |
| v2.0 | 重大修订 | 多个评测冲突积累后的全面更新 |

- 每个版本保留完整的根因列表和排查步骤快照
- 可回溯对比任意两个版本
- 版本历史记录在教材详情页查看

### 7.9 闭环流程图

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│  标注页面  │────→│  智能体   │────→│  评测页面  │
│ (创建教材) │     │ (实战使用) │     │ (测试诊断) │
└─────▲────┘     └──────────┘     └─────┬────┘
      │                                   │
      │          发现冲突，生成修订任务        │
      │         ┌──────────────┐          │
      └─────────│  修订任务队列   │◄─────────┘
                │ (待专家审核)    │
                └──────┬───────┘
                       │ 专家选择修订模式
                  ┌────┴────┐
                  │         │
           ┌──────┘    ┌────┘
           │           │
    快速审核模式    对话式修订模式
    (表单勾选)      (AI访谈引导)
           │           │
           └─────┬─────┘
                 │
          ┌──────┴──────┐
          │ 修订结果确认  │
          └──────┬──────┘
                 │
        ┌────────┼────────┐
        │        │        │
     接受合并   拒绝关闭  修改后合并
        │        │        │
     版本+1   记录原因   调整后版本+1
```

---

## 8. 后续规划

| 功能 | 优先级 | 说明 |
|------|--------|------|
| 用户登录与权限 | P1 | 支持专家/管理员角色区分 |
| 标注历史记录 | P1 | 查看某条教材的历史标注记录和变更对比 |
| 多人协作标注 | P2 | 支持多人同时标注不同故障，锁定机制防冲突 |
| 标注质量评分 | P2 | 基于标注覆盖率、修改率等维度自动评分 |
| 教材版本管理 | P2 | 完整的版本历史和 diff 对比 |
| 批量导入故障 | P3 | 支持从 Excel/CSV 批量导入故障现象 |
| 导出标注结果 | P3 | 支持导出为 PDF/Word 格式的故障教材 |
| 移动端适配 | P3 | 响应式布局优化，支持平板操作 |
