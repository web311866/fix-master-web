# 故障教材评测平台 PRD

## 1. 产品概述

### 1.1 产品背景

故障教材标注平台已完成教材的编写与专家审核，但教材质量最终需要通过**实战验证**来衡量。当前缺乏一个系统化的评测工具，让专家能够基于智能体真实对话产生的故障规划报告，对教材的实际效果进行回溯评估。

本产品旨在搭建一个**故障教材评测闭环**：从智能体会话中提取真实生成的故障规划报告，由领域专家逐模块标注报告中的错误与遗漏，为算法工程师提供精准的改进依据。

### 1.2 产品定位

面向领域专家的**故障教材效果评测与标注工具**，核心价值：

- 将智能体实战输出的故障规划报告可视化呈现，降低评测门槛
- 通过两阶段标注机制（根因评测 + 排查路径评测），精准定位教材不足
- 为算法工程师提供结构化的错误归因数据，驱动教材迭代优化

### 1.3 目标用户

| 角色 | 描述 | 核心诉求 |
|------|------|----------|
| 领域专家 | 工程机械维修/诊断方向的资深工程师 | 高效评测 AI 诊断报告的准确性，标注错误与遗漏 |
| 算法工程师 | 负责故障诊断模型/智能体开发优化的工程师 | 获取结构化的错误归因数据，分析错误模式，改进算法 |
| 教材管理员 | 负责教材质量管控的管理人员 | 追踪评测进度、统计评测质量、对比标注与评测结果 |

### 1.4 与标注平台的关系

| 维度 | 标注平台 | 评测平台 |
|------|----------|----------|
| 输入 | AI 生成的教材初稿 | 智能体实战生成的故障规划报告 |
| 目的 | 确保教材内容准确、完整 | 验证教材在实战中的效果 |
| 标注对象 | 根因列表、剪枝规则、维修建议 | 故障报告中的根因、排查步骤/路径 |
| 产出 | 高质量教材 | 错误归因数据 + 改进建议 |
| 关系 | 评测的前置依赖 | 评测结果反哺教材优化 |

---

## 2. 产品架构

### 2.1 整体流程

```
选择产品大类 → 选择故障现象 → 加载智能体会话记录 → 提取故障规划报告 → 第一阶段：根因评测 → 第二阶段：排查路径评测 → 完成提交
     ①              ②                ③                    ④                    ⑤                     ⑥                    ⑦
```

### 2.2 页面结构

| 页面/步骤 | 功能 | 核心交互 |
|-----------|------|----------|
| Step 1 选择产品大类 | 选择设备类型 | 卡片点选（复用标注平台大类） |
| Step 2 选择故障现象 | 选择待评测的故障 | 标签列表 + 搜索筛选 |
| Step 3 加载会话记录 | 展示智能体历史会话 | 对话气泡 + 滚动浏览 + 一键提取报告 |
| Step 4 提取故障报告 | 解析并展示故障规划报告 | 报告卡片（4模块） + 自动定位 |
| Step 5 根因评测 | 标注根因列表中的错误 | 逐条标注（遗漏/非必要/错误） + 补充正确根因 |
| Step 6 排查路径评测 | 标注排查步骤中的错误路径 | 逐步骤标注（错误路径 + 正确路径） + 原因说明 |
| Step 7 完成提交 | 汇总评测结果 | 评测统计 + 生成改进报告 + 继续下一个 |

---

## 3. 功能详设

### 3.1 Step 1：选择产品大类

#### 3.1.1 功能描述

复用标注平台的产品大类体系，确保评测与标注使用同一分类标准。

#### 3.1.2 产品大类列表

| 大类 | 图标 | 说明 |
|------|------|------|
| 挖掘机 | 🚜 | 液压挖掘机、微型挖掘机等 |
| 泵车 | 🚛 | 混凝土泵车、车载泵等 |
| 起重机 | 🏗️ | 汽车起重机、履带起重机等 |
| 装载机 | 🚧 | 轮式装载机、滑移装载机等 |
| 摊铺机 | 🛣️ | 沥青摊铺机等 |
| 压路机 | 🪨 | 单钢轮压路机、双钢轮压路机等 |

#### 3.1.3 交互规则

- 点击大类卡片即选中，高亮显示
- 选中后自动进入 Step 2
- 页面顶部显示当前选中的大类，支持点击切换

---

### 3.2 Step 2：选择故障现象

#### 3.2.1 功能描述

从已有故障教材中选择待评测的故障现象，仅展示已有教材和会话记录的故障。

#### 3.2.2 交互规则

| 规则 | 说明 |
|------|------|
| 列表来源 | 仅展示已完成标注的故障现象（有教材可用） |
| 会话标识 | 有智能体会话记录的故障显示"可评测"标签，无记录的置灰不可选 |
| 搜索 | 支持关键词搜索过滤 |
| 选中 | 点击标签选中，自动进入 Step 3 |
| 多选 | 不支持，每次评测一个故障现象 |

#### 3.2.3 故障标签状态

| 状态 | 样式 | 说明 |
|------|------|------|
| 可评测 | 蓝色实心标签 + 📋 图标 | 有教材 + 有会话记录 |
| 仅有教材 | 灰色标签 + 📖 图标 | 有教材但无会话记录，不可选 |
| 已评测 | 绿色标签 + ✅ 图标 | 已完成本次评测 |

---

### 3.3 Step 3：加载智能体会话记录

#### 3.3.1 功能描述

展示智能体与用户的历史对话记录，从中定位并提取最终生成的故障规划报告。

#### 3.3.2 会话记录展示

| 元素 | 说明 |
|------|------|
| 对话气泡 | 区分用户消息（右侧蓝色）和智能体消息（左侧灰色） |
| 时间戳 | 每条消息的发送时间 |
| 会话元数据 | 会话ID、开始时间、持续时长、消息条数 |
| 报告定位标记 | 智能体生成最终报告的消息标记为"📌 故障规划报告" |

#### 3.3.3 交互规则

| 规则 | 说明 |
|------|------|
| 自动定位 | 页面自动滚动到报告生成的位置 |
| 提取按钮 | 点击"提取故障报告"按钮，系统解析报告内容 |
| 手动选择 | 支持用户手动选择一段消息作为报告来源 |
| 多份报告 | 如果会话中有多份报告，展示列表供选择 |

#### 3.3.4 数据结构

```json
{
  "sessionId": "conv_20260503_001",
  "startTime": "2026-05-03 10:30:00",
  "duration": "12分35秒",
  "messageCount": 28,
  "messages": [
    {
      "role": "user",
      "content": "我的挖掘机行走无力，右侧更明显",
      "timestamp": "2026-05-03 10:30:15"
    },
    {
      "role": "assistant",
      "content": "根据您描述的症状...",
      "timestamp": "2026-05-03 10:30:22",
      "isReportGeneration": true
    }
  ]
}
```

---

### 3.4 Step 4：提取故障规划报告

#### 3.4.1 功能描述

从会话记录中解析并结构化展示故障规划报告的四个模块。

#### 3.4.2 报告模块

| 模块 | 说明 | 展示形式 |
|------|------|----------|
| 故障现象标题 | 报告识别出的故障名称 | 文本 |
| 故障现象描述 | 对故障症状的详细描述 | 文本段落 |
| 故障可能根因 | 列出所有可能的根因 | 编号列表，每条含置信度 |
| 故障排查步骤/流程图 | 排查的步骤序列或决策流程 | 步骤卡片 + 流程图 |

#### 3.4.3 报告数据结构

```json
{
  "reportId": "rpt_20260503_001",
  "faultTitle": "挖掘机行走无力",
  "faultDescription": "挖掘机在平地上行走速度明显下降，右侧履带较左侧更为无力，伴随液压系统异常噪音...",
  "rootCauses": [
    {
      "id": "rc_01",
      "content": "行走马达溢流阀故障",
      "confidence": 0.92
    },
    {
      "id": "rc_02",
      "content": "主泵先导压力不足",
      "confidence": 0.85
    }
  ],
  "troubleshootingSteps": [
    {
      "id": "ts_01",
      "stepNumber": 1,
      "title": "检查行走先导压力",
      "description": "连接压力表检测行走先导压力是否在正常范围(2.5-3.9MPa)",
      "expectedResult": "先导压力正常",
      "nextStepIfPass": "ts_02",
      "nextStepIfFail": "ts_03"
    }
  ]
}
```

#### 3.4.4 交互规则

| 规则 | 说明 |
|------|------|
| 报告预览 | 以卡片形式展示四个模块的内容概要 |
| 展开详情 | 点击模块卡片展开完整内容 |
| 报告来源 | 标注报告来源的会话ID和生成时间 |
| 确认提取 | 用户确认报告内容正确后，进入标注阶段 |

---

### 3.5 Step 5：第一阶段 - 根因评测

#### 3.5.1 功能描述

对故障规划报告中的"故障可能根因"列表逐条评测，标注三类问题：遗漏根因、非必要根因、错误根因。

#### 3.5.2 标注类型定义

| 标注类型 | 定义 | 标注方式 | 示例 |
|----------|------|----------|------|
| 遗漏根因 | 报告中未列出但应该包含的根因 | 点击"添加遗漏根因"按钮，填写根因内容 + 必要性说明 | 报告遗漏了"行走制动阀未完全释放" |
| 非必要根因 | 报告中列出但实际与当前故障无关的根因 | 对已有根因标记为"非必要" + 填写理由 | "液压油污染"与本次特定故障无关 |
| 错误根因 | 报告中列出且描述错误或方向完全错误的根因 | 对已有根因标记为"错误" + 填写正确内容 | "行走比例电磁阀故障"应为"行走比例减压阀故障" |

#### 3.5.3 根因标注交互

**已有根因列表：**

每条根因展示为一行，包含：
- 根因编号 + 内容
- AI 置信度标签
- 三个操作按钮：✅ 正确 | ⚠️ 非必要 | ❌ 错误

**操作流程：**

1. 专家逐条审阅根因
2. 点击对应按钮标记
3. 标记为"非必要"时，弹出理由输入框
4. 标记为"错误"时，弹出理由输入框 + 正确根因输入框
5. 标记为"正确"时，无需额外输入

**遗漏根因添加：**

- 底部"添加遗漏根因"按钮
- 点击弹出表单：根因内容 + 必要性说明 + 关联故障症状
- 支持添加多条

#### 3.5.4 标注数据结构

```json
{
  "rootCauseAnnotations": [
    {
      "rootCauseId": "rc_01",
      "content": "行走马达溢流阀故障",
      "verdict": "correct"
    },
    {
      "rootCauseId": "rc_02",
      "content": "主泵先导压力不足",
      "verdict": "unnecessary",
      "reason": "先导压力不足主要影响主泵排量控制，与行走无力直接关联度低"
    },
    {
      "rootCauseId": "rc_05",
      "content": "液压油污染或变质",
      "verdict": "incorrect",
      "reason": "本次故障已确认为机械故障而非油液问题",
      "correction": "液压泵伺服活塞挡块O型圈损坏导致内泄"
    }
  ],
  "missingRootCauses": [
    {
      "content": "行走制动阀未完全释放",
      "necessity": "制动阀卡滞会导致持续制动，表现为行走无力",
      "relatedSymptoms": ["行走无力", "起步困难"]
    }
  ]
}
```

#### 3.5.5 标注统计

| 指标 | 说明 |
|------|------|
| 正确根因数 | 标记为"正确"的根因数量 |
| 非必要根因数 | 标记为"非必要"的根因数量 |
| 错误根因数 | 标记为"错误"的根因数量 |
| 遗漏根因数 | 专家补充的遗漏根因数量 |
| 根因准确率 | 正确根因数 / (正确 + 非必要 + 错误) |

---

### 3.6 Step 6：第二阶段 - 排查路径评测

#### 3.6.1 功能描述

对故障规划报告中的排查步骤逐步骤评测，标注错误路径并指出正确路径，为算法工程师提供决策优化的依据。

#### 3.6.2 标注维度

| 维度 | 说明 |
|------|------|
| 错误路径 | 该步骤的排查方向错误，会导致诊断偏移 |
| 正确路径 | 应当采取的正确排查方向 |
| 错误原因 | 路径错误的原因分类（见下方） |
| 改进建议 | 对排查逻辑的具体改进建议 |

#### 3.6.3 错误原因分类

| 分类 | 定义 | 示例 |
|------|------|------|
| 顺序错误 | 排查优先级不合理，应先排查更可能的原因 | 应先检查行走先导压力而非直接拆解马达 |
| 条件错误 | 判断条件（通过/失败阈值）设置不当 | 先导压力正常范围判断标准有误 |
| 遗漏步骤 | 缺少关键排查步骤 | 遗漏了"对比左右行走压力差"步骤 |
| 冗余步骤 | 不必要的排查步骤增加了诊断时长 | 在确认先导压力正常后仍要求检查先导泵 |
| 结论错误 | 步骤结论与实际不符 | 根据压力值判断为马达故障，实际为溢流阀故障 |

#### 3.6.4 排查步骤标注交互

**步骤展示：**

每个排查步骤展示为一张卡片，包含：
- 步骤编号 + 标题
- 步骤描述
- 预期结果
- 跳转逻辑（通过→步骤X / 失败→步骤Y）

**标注操作：**

1. 专家审阅每个步骤
2. 点击"✅ 路径正确"或"❌ 路径错误"
3. 标记路径错误时，展开标注表单：
   - 错误原因分类（下拉选择）
   - 正确路径描述（文本输入）
   - 详细说明（文本域）
4. 标记路径正确时，可选择性添加备注

**路径对比视图：**

标注完成后可切换为"路径对比"视图：
- 左侧：报告原始路径（标红错误步骤）
- 右侧：专家修正路径（绿色正确步骤）
- 高亮差异部分

#### 3.6.5 标注数据结构

```json
{
  "stepAnnotations": [
    {
      "stepId": "ts_01",
      "stepTitle": "检查行走先导压力",
      "verdict": "correct",
      "note": ""
    },
    {
      "stepId": "ts_02",
      "stepTitle": "检查行走马达溢流阀",
      "verdict": "incorrect",
      "errorType": "sequence_error",
      "correctPath": "应先对比左右行走压力差，定位是单侧还是双侧问题，再决定拆解方向",
      "detail": "在未确认是单侧故障还是双侧故障时，直接拆解溢流阀效率低。应先进行左右压力对比测试。",
      "correctNextStep": "先执行左右行走压力对比测试"
    },
    {
      "stepId": "ts_04",
      "stepTitle": "检查液压油品质",
      "verdict": "incorrect",
      "errorType": "redundant_step",
      "correctPath": "在已确认先导压力和溢流阀正常的情况下，应检查中央回转接头油封",
      "detail": "此步骤在当前诊断链路上是冗余的，油液品质问题概率已极低"
    }
  ]
}
```

#### 3.6.6 标注统计

| 指标 | 说明 |
|------|------|
| 路径正确步骤数 | 标记为"路径正确"的步骤数 |
| 路径错误步骤数 | 标记为"路径错误"的步骤数 |
| 错误类型分布 | 各类错误原因的数量统计 |
| 路径准确率 | 正确步骤数 / 总步骤数 |

---

### 3.7 Step 7：完成提交

#### 3.7.1 功能描述

汇总两个阶段的评测结果，生成评测报告，支持查看详情或继续评测下一个故障。

#### 3.7.2 评测报告内容

| 模块 | 内容 |
|------|------|
| 基本信息 | 产品大类、故障现象、评测人、评测时间 |
| 根因评测汇总 | 准确率、三类问题统计、遗漏根因列表 |
| 路径评测汇总 | 路径准确率、错误类型分布、关键错误步骤 |
| 综合评分 | 根因准确率 × 0.6 + 路径准确率 × 0.4 |
| 改进建议 | 自动汇总专家标注中的改进建议 |

#### 3.7.3 综合评分规则

```
综合评分 = 根因准确率 × 0.6 + 路径准确率 × 0.4

其中：
- 根因准确率 = 正确根因数 / (正确 + 非必要 + 错误根因数)
- 路径准确率 = 路径正确步骤数 / 总步骤数

评分等级：
- A (≥ 0.9)：优秀，教材质量高
- B (0.7-0.89)：良好，存在少量改进点
- C (0.5-0.69)：一般，需要较多改进
- D (< 0.5)：较差，需重点重构
```

#### 3.7.4 交互规则

| 规则 | 说明 |
|------|------|
| 查看详情 | 点击展开完整评测数据 |
| 导出报告 | 导出为 JSON/PDF 格式 |
| 继续评测 | 返回 Step 2 选择下一个故障现象 |
| 评测列表 | 首页展示已完成的评测记录列表 |

---

## 4. 全局交互规范

### 4.1 步骤导航

- 页面顶部显示步骤进度条（7 步），当前步骤高亮
- 已完成步骤显示 ✅，支持点击回看
- 未完成步骤置灰，不可跳转
- 每步完成后自动跳转下一步

### 4.2 弹窗规范

| 场景 | 弹窗类型 | 操作 |
|------|----------|------|
| 标记非必要根因 | 输入弹窗 | 填写理由 → 确认 |
| 标记错误根因 | 输入弹窗 | 填写理由 + 正确内容 → 确认 |
| 添加遗漏根因 | 表单弹窗 | 填写内容 + 说明 → 确认 |
| 标记错误路径 | 展开式表单 | 选择错误类型 + 填写正确路径 → 确认 |

### 4.3 Toast 通知

| 场景 | 类型 | 文案示例 |
|------|------|----------|
| 标注成功 | 成功（绿色） | "已标记为非必要根因" |
| 提交成功 | 成功（绿色） | "评测结果已提交" |
| 数据异常 | 警告（橙色） | "报告解析失败，请手动选择" |
| 操作冲突 | 错误（红色） | "该根因已标注，请先取消再重新标注" |

### 4.4 确认机制

| 场景 | 确认方式 |
|------|----------|
| 提交评测结果 | 二次确认弹窗 |
| 取消已完成的标注 | 二次确认弹窗 |
| 离开评测页面 | 二次确认弹窗（未保存提示） |

---

## 5. UI 设计规范

### 5.1 主题色

| 用途 | 色值 | 说明 |
|------|------|------|
| 主背景 | #F8FAFC | 浅灰白 |
| 卡片背景 | #FFFFFF | 纯白 |
| 主强调色 | #3B82F6 | 蓝色 |
| 正确/通过 | #10B981 | 绿色 |
| 警告/非必要 | #F59E0B | 橙色 |
| 错误/遗漏 | #EF4444 | 红色 |
| 主文字 | #1E293B | 深灰 |
| 辅助文字 | #64748B | 中灰 |
| 边框 | #E2E8F0 | 浅灰 |

### 5.2 标注状态颜色

| 状态 | 背景 | 边框 | 文字 |
|------|------|------|------|
| 正确 | #ECFDF5 | #10B981 | #065F46 |
| 非必要 | #FFFBEB | #F59E0B | #92400E |
| 错误 | #FEF2F2 | #EF4444 | #991B1B |
| 遗漏 | #EFF6FF | #3B82F6 | #1E40AF |
| 未标注 | #F8FAFC | #E2E8F0 | #64748B |

### 5.3 响应式断点

| 断点 | 宽度 | 布局 |
|------|------|------|
| 桌面 | ≥ 1024px | 双栏布局 |
| 平板 | 768-1023px | 单栏，步骤导航折叠 |
| 手机 | < 768px | 单栏，简化视图 |

---

## 6. 数据流转

### 6.1 核心数据流

```
智能体会话记录 → 报告提取引擎 → 结构化报告 → 专家标注 → 评测结果 → 算法优化反馈
```

### 6.2 数据来源

| 数据 | 来源 | 说明 |
|------|------|------|
| 会话记录 | 智能体对话引擎 | 用户与智能体的历史对话 |
| 故障规划报告 | 从会话中提取 | 智能体生成的最终诊断报告 |
| 故障教材 | 标注平台 | 已完成标注的教材数据 |
| 标注规则 | 本系统内置 | 根因/路径的标注类型定义 |

### 6.3 数据输出

| 输出 | 格式 | 消费方 |
|------|------|--------|
| 根因评测结果 | JSON | 算法工程师（根因模型优化） |
| 路径评测结果 | JSON | 算法工程师（决策树优化） |
| 综合评测报告 | JSON/PDF | 教材管理员（质量监控） |
| 改进建议 | JSON | 知识工程师（教材迭代） |

---

## 7. 修订任务生成与对话式修订

### 7.1 概述

评测完成后，专家可基于评测标注结果生成修订任务，将评测发现的问题反哺回标注平台进行教材修订。修订任务支持两种模式：快速审核和对话式修订。

### 7.2 修订任务生成

#### 7.2.1 触发时机

评测完成后（Step 7），在完成页面增加"生成修订任务"操作区。

#### 7.2.2 自动生成规则

系统根据评测标注自动转化为修订建议：

| 评测标注类型 | 转化为修订建议 | 修订操作 |
|------------|------------|---------|
| 错误根因 | 根因冲突 - 错误 | 建议删除或修改该根因 |
| 非必要根因 | 根因冲突 - 非必要 | 建议删除或降级该根因 |
| 遗漏根因 | 根因冲突 - 遗漏 | 建议新增该根因到教材 |
| 路径错误（顺序错误） | 路径冲突 - 顺序 | 建议调整步骤顺序 |
| 路径错误（条件错误） | 路径冲突 - 条件 | 建议修改判断条件 |
| 路径错误（遗漏步骤） | 路径冲突 - 遗漏 | 建议补充排查步骤 |
| 路径错误（冗余步骤） | 路径冲突 - 冗余 | 建议删除该步骤 |
| 路径错误（结论错误） | 路径冲突 - 结论 | 建议修正步骤结论 |

#### 7.2.3 修订任务预览

生成修订任务前，系统展示预览列表：

```
┌──────────────────────────────────────────────┐
│ 📋 修订任务预览 - 液压系统行走无力              │
│                                              │
│ 根因相关（3条）：                              │
│ 1. [删除] "液压油污染或变质" - 评测标记为非必要  │
│ 2. [修改] "主泵先导压力不足" → 应为先导油路故障  │
│ 3. [新增] "行走制动阀未完全释放" - 评测遗漏补充  │
│                                              │
│ 路径相关（2条）：                              │
│ 4. [顺序] 步骤3应在步骤2之前执行               │
│ 5. [遗漏] 步骤5后应增加"对比左右行走压力差"     │
│                                              │
│ ────────────────────────────                 │
│ 选择修订模式：                                 │
│ ┌──────────────┐  ┌──────────────┐           │
│ │  📋 快速审核   │  │  💬 对话式修订 │           │
│ │  逐条勾选确认  │  │  AI引导访谈   │           │
│ └──────────────┘  └──────────────┘           │
│                                              │
│ [生成并跳转标注平台]                           │
└──────────────────────────────────────────────┘
```

#### 7.2.4 交互规则

| 规则 | 说明 |
|------|------|
| 自动生成 | 系统自动将评测标注转化为修订建议，无需手动创建 |
| 预览编辑 | 提交前可预览和编辑修订任务内容 |
| 模式选择 | 选择"快速审核"或"对话式修订"模式 |
| 跳转标注 | 点击"生成并跳转"后，跳转到标注平台的修订模式 |
| 状态追踪 | 已生成的修订任务在评测页面可查看处理状态 |

### 7.3 对话式修订

#### 7.3.1 入口

在修订任务预览中选择"对话式修订"模式，进入对话式修订界面。

#### 7.3.2 界面布局

- 左侧：修订任务导航列表（显示所有冲突项，可点击跳转）
- 右侧：AI 对话区域（主要交互区）
- 底部：输入框 + 发送按钮 + 快捷操作栏

#### 7.3.3 AI 访谈员行为规范

| 规范 | 说明 |
|------|------|
| 主动引导 | AI 逐条推送修订任务，每次展示评测证据后提问 |
| 自然语言理解 | 专家可以用自然语言回复，AI 解析意图 |
| 结构化复述 | 专家表达后，AI 用结构化格式复述确认 |
| 嵌入操作 | 确认按钮内嵌在对话流中，不跳出弹窗 |
| 阶段总结 | 每个模块结束后汇总所有修订 |

#### 7.3.4 对话消息类型

| 消息类型 | 来源 | 内容 |
|---------|------|------|
| 证据展示 | AI | 展示评测标注和会话证据 |
| 提问引导 | AI | 针对当前冲突向专家提问 |
| 结构化复述 | AI | 将专家回复解析为结构化修订建议 |
| 自由回复 | 专家 | 自然语言表达意见 |
| 确认操作 | 专家 | 确认/调整/拒绝修订建议 |
| 阶段总结 | AI | 汇总当前模块所有修订 |

### 7.4 修订任务状态流转

```
待处理(pending) → 审核中(in_review) → 已接受(accepted) → 已合并(merged)
                                   → 已拒绝(rejected)
                                   → 修改中(modifying) → 已接受 → 已合并
```

| 状态 | 说明 |
|------|------|
| pending | 刚从评测生成，等待专家处理 |
| in_review | 专家正在审核（快速审核或对话式修订中） |
| accepted | 专家已接受修订建议 |
| rejected | 专家已拒绝修订建议 |
| modifying | 专家正在修改建议内容 |
| merged | 修订已合并到教材，版本号已更新 |

### 7.5 评测页面新增交互

#### 7.5.1 Step 7 完成页面新增区域

在现有完成页面底部增加"修订任务"操作区：

| 元素 | 说明 |
|------|------|
| 冲突发现提示 | "本次评测发现 N 处冲突，可生成修订任务反哺教材优化" |
| 修订任务预览按钮 | 点击展开预览列表 |
| 模式选择 | 快速审核 / 对话式修订 两种模式卡片 |
| 生成按钮 | "生成修订任务并跳转标注平台" |
| 已生成任务列表 | 展示已生成的修订任务及其处理状态 |

---

## 8. 后续规划

### 7.1 P1（核心功能）

| 功能 | 说明 |
|------|------|
| 会话记录加载 | 接入真实智能体会话数据 |
| 报告自动提取 | NLP 自动识别会话中的报告生成节点 |
| 两阶段标注 | 根因评测 + 路径评测完整流程 |
| 评测报告生成 | 自动汇总统计 + 评分 |

### 7.2 P2（体验优化）

| 功能 | 说明 |
|------|------|
| 路径对比视图 | 原始路径 vs 修正路径的可视化对比 |
| 批量评测 | 支持一次导入多个会话记录 |
| 评测模板 | 针对不同大类预设评测侧重点 |
| 历史对比 | 同一故障不同版本的评测结果对比 |

### 7.3 P3（高级功能）

| 功能 | 说明 |
|------|------|
| 多人评测 | 同一报告支持多位专家独立评测 |
| 一致性分析 | 多位专家评测结果的一致性统计 |
| 自动预标注 | 基于历史标注数据自动预填评测结果 |
| 与标注平台联动 | 评测发现的问题自动创建标注任务 |
| 错误模式挖掘 | 聚合多份评测报告，发现系统性错误模式 |
